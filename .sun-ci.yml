default:
  workspace: shared

stages:
- Build
- Test

jobs:
- name: Run tests
  stage: Build
  image: alpine
  script:
  - echo OK
  only:
    branches:
    - test-only
    events:
    - pull_request
